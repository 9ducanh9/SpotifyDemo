name: music_backend
description: A Dart backend prototype for music app authentication and PostgreSQL APIs.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.1

dependencies:
  path: ^1.9.0
  postgres: ^3.0.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  bcrypt: ^1.1.3
  dart_jsonwebtoken: ^2.4.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
